{% extends 'baseAdmin.html.twig' %}
{% block content %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title" style="font-size: 20px;"> Categorie numéro {{ category.id }} </strong>
                        </div>
                        <div class="card-body">
                            <div class="vue-lists">
                                <div class="row">
                                    <div class="col-md-6">
                                        <ul style="font-size: 16px;">
                                            <li>
                                                <strong> Nom categorie : </strong> {{ category.title }}
                                            </li>
                                            <li>  <strong> Description categorie : </strong> {{ category.description }}</li>
                                        </ul>
                                    </div>

                                </div>
                            </div>

                            <table>
                                <tr>
                                    <td>          <a href="{{ path('category_edit', { 'id': category.id }) }}" class="btn btn-info btn-xs"><i class="fa fa-pencil"></i> Edit</a>
                                    </td>
                                    <td>

                                        {{ form_start(delete_form) }}
                                        <input type="submit" value="Supprimer" class="btn btn-danger btn-xs">
                                        {{ form_end(delete_form) }}</td>
                                </tr>
                            </table>
                        </div></div></div></div></div>
        <a href="{{ path('category_index') }}" class="btn btn-outline-secondary">Liste des categories</a>
    </div>
{% endblock %}